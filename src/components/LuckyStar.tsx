// Kept as a thin no-op wrapper for older imports. The active roaming
// star field lives in <StarField /> (see src/components/StarField.tsx).
export function LuckyStar(_props: { scope?: "page" | "results" }) {
  return null;
}
